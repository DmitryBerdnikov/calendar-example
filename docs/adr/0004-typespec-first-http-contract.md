# TypeSpec-first HTTP contract

The HTTP API contract is authored in TypeSpec, emitted as OpenAPI, and used to generate frontend TypeScript client/types. This keeps the frontend mock, Swagger inspection, and backend implementation aligned around one contract before application code is built.
