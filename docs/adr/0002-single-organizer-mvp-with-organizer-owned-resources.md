# Single-organizer MVP with organizer-owned resources

The MVP supports one provisioned organizer, but event types, availability, and bookings are still modeled as organizer-owned resources. This keeps the first implementation small while avoiding a later rewrite of the core API shape if multiple organizers are added.
