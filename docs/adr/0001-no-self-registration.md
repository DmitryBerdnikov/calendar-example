# No self-registration

The MVP supports organizer sign-in but does not support public self-registration. Organizer identities are provisioned ahead of time so the first slice can focus on scheduling behavior, while guests remain unauthenticated and can only use public booking flows.
