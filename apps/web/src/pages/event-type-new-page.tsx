import { PageShell } from "../shared/ui/page-shell";

export function EventTypeNewPage() {
  return (
    <PageShell
      title="New Event Type"
      description="Create form fields will be added in the event type slice."
    />
  );
}
